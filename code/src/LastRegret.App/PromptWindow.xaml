<Window x:Class="LastRegret.App.PromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="输入"
        Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{StaticResource BgPanel}"
        TextOptions.TextFormattingMode="Display">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptText" Grid.Row="0" TextWrapping="Wrap" FontSize="12.5" Margin="0,0,0,10" />

        <TextBox x:Name="InputBox" Grid.Row="1" MinWidth="380" MaxLength="200"
                 KeyDown="OnInputKeyDown" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="清空" Click="OnClearClick" Margin="0,0,8,0" />
            <Button Content="取消" Click="OnCancelClick" Margin="0,0,8,0" IsCancel="True" />
            <Button Content="确定" Style="{StaticResource PrimaryButton}" Click="OnOkClick" IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
